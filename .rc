#!/usr/bin/env sh

# Load dev/ rc files
OPWD="$(pwd)" ; cd dev ; . ./rc ; cd "$OPWD"

# Install frozen base dependencies. This installs poetry.
pip install -r requirements.txt

# Install poetry-managed packages in Poetry's virtualenv
poetry install
